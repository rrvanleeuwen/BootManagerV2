@page "/onboarding"
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "Owner")]

<h3>Onboarding - Applicatie Instellen</h3>

<div class="alert alert-info">
    <h5>Onboarding Vereist</h5>
    <p>
        Welkom! Voordat je BootManager kunt gebruiken, moet je eerst de initiële setup voltooien.
        Dit omvat het instellen van je eigenaargegevens en bootinformatie.
    </p>
    <p>
        <strong>Placeholder:</strong> Het volledige onboardingformulier wordt in de volgende iteratie (US4) geïmplementeerd.
    </p>
</div>

<p>
    Zodra de onboarding is voltooid, krijg je toegang tot het dashboard en alle overige functies van BootManager.
</p>
