# Copy this file to .env on the deployment host and replace both values.
# Keep .env out of git.

BOOTMANAGER_ENCRYPTION_KEY=replace-with-a-long-random-encryption-key
BOOTMANAGER_JWT_KEY=replace-with-a-long-random-jwt-signing-key
